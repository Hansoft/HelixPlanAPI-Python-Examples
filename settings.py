# -*- coding: utf-8 -*-
""" Settings to be used for the Helix Plan API.
'dev' environment is used by default by the scripts.
"""
CONNECTION = {
    'prod': {
        # Add the URL to the API like https://helixplan.example.com/api/graphql
        'URL': '',
        # The regular Helix Plan username and password to run the script as.
        'USER': '',
        'PASSWORD': '',
    },
    'dev': {
        # Add the URL to the API like https://helixplan.example.com/api/graphql
        'URL': '',
        # The regular Helix Plan username and password to run the script as.
        'USER': '',
        'PASSWORD': '',
    }
}
